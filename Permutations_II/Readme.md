- PS:我发现我在为知笔记MD文件类型有些好像都用不了，比如说# or [TOC]
- 先贴上链接：<http://www.lintcode.com/zh-cn/problem/permutations-ii/>
- 我的分析 or 总结
- 1.这道题我一上来就想用的是全排列算法，然后这道题就转化为怎么去将带重复元素的排列删除，我之前想到了两种方案，
- 一种就是比较笨的，在边界输出到list<list>那里用for循环判断查找，当然这种方案我尝试了一下，就之前 time limit 了
- 另一种就是在规则上加入判断条件
```
if(i!=k&&a.get(i)==a.get(k))return;
```
- 但是这种被证实也是错误的。因为会把不该排除的排除了，达不到筛选效果。
- so,我还是打算用第一种，但是，我加入了一个数据结构——哈希表，就把本来for循环的时间复杂度O(n)变成了O（1），测试完全通过
- 无敌哈希表carry全场呀